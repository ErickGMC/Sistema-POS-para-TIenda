const { initializeApp } = require('firebase/app'); const { getFirestore, collection, getDocs, query, limit } = require('firebase/firestore'); const fs = require('fs'); const path = require('path'); const configPath = path.join(__dirname, '../electron/sync/firebase_config.json'); if (!fs.existsSync(configPath)) { console.error('Config not found at', configPath); process.exit(1); } const config = JSON.parse(fs.readFileSync(configPath, 'utf8')); const app = initializeApp(config); const db = getFirestore(app); async function checkAnalytics() { console.log('Checking analytics collection...'); try { const snap1 = await getDocs(query(collection(db, 'analytics'), limit(5))); console.log(Found  docs in analytics); snap1.docs.forEach(d => console.log(d.id, d.data())); } catch (e) { console.error('Error querying analytics:', e.message); } console.log('Checking analytic collection...'); try { const snap2 = await getDocs(query(collection(db, 'analytic'), limit(5))); console.log(Found  docs in analytic); snap2.docs.forEach(d => console.log(d.id, d.data())); } catch (e) { console.error('Error querying analytic:', e.message); } process.exit(0); } checkAnalytics();
